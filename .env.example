# Copy to .env and fill in real values. .env is git-ignored.
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_READ_TOKEN=
SANITY_WRITE_TOKEN=
# Editor-permission token, used ONLY by api/talent-submission.js on the server.
# Add it in Vercel: Project -> Settings -> Environment Variables. Never commit it.
